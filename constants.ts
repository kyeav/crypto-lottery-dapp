export const currency: string = "MATIC";

// const { contract, isLoading, error } = useContract("0xB771dF889e678ab9d1D38EcC00fF58A6Ca221F17");

// const { contract } = useContract(
//   process.env.NEXT_PUBLIC_LOTTERY_CONTRACT_ADDRESS
// );

// export const { data: remainingTickets } = useContractRead(
//   contract,
//   "RemainingTickets"
// );

// export const { data: currentWinningReward } = useContractRead(
//   contract,
//   "CurrentWinningReward"
// );

// export const { data: ticketPrice } = useContractRead(contract, "ticketPrice");

// export const { data: ticketCommission } = useContractRead(
//   contract,
//   "ticketCommission"
// );

// export const { data: expiration } = useContractRead(contract, "expiration");
